# Getting Started

Clone the repository and install the dependencies. Then copy the example
configuration file and adjust the connection strings for your environment.

Run the migrations before starting the server for the first time.
